<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/background_splash"
    tools:context=".SplashScreen">

    <TextView
        android:layout_alignParentTop="true"
        android:id="@+id/text_view"
        android:gravity="center_vertical|center"
        android:textSize="40sp"
        android:layout_marginStart="35dp"
        android:layout_marginEnd="35dp"
        android:textColor="@color/white"
        android:layout_marginTop="45dp"
        android:textStyle="bold"
        android:lineSpacingExtra="10dp"
        android:text="Selamat Datang di Aplikasi MyDoa"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"/>

    <ImageView
        android:layout_above="@id/progress_circular"
        android:id="@+id/image"
        android:padding="90dp"
        android:layout_below="@id/text_view"
        android:src="@drawable/logo_aplikasi"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"/>

    <ProgressBar
        android:layout_above="@id/bykelompok"
        android:layout_marginBottom="5dp"
        android:id="@+id/progress_circular"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"/>

    <TextView
        android:id="@+id/bykelompok"
        android:gravity="center_vertical|center"
        android:layout_alignParentBottom="true"
        android:textSize="15sp"
        android:textColor="@color/white"
        android:layout_marginTop="40dp"
        android:textStyle="bold"
        android:layout_marginBottom="20dp"
        android:lineHeight="50sp"
        android:text="#selaluberdoa"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"/>

</RelativeLayout>